version: "3.3"

services: 
  app: 
    build: .
    image: seocahill/dts-explorer-client:debian
    volumes:
      - .:/src:cached
      - node_modules:/src/node_modules:cached
    ports:
      - "4200:4200"
      - "7020:7020"
      - "7357:7357"
      - "9222:9222"

volumes:
  node_modules: